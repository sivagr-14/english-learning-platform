{
  "schemaVersion": 1,
  "featureId": "fast-update-startup",
  "title": "Complete application updates without a control-service handoff",
  "implementationPaths": [
    "scripts/control-server.js"
  ],
  "validationPaths": [
    "scripts/control-server.test.js"
  ],
  "chatgptInstructionImpact": {
    "status": "not_required",
    "rationale": "This removes the fragile mid-update controller handoff so the running controller completes migrations, content synchronization, and web-service startup. Content-pack and vocabulary-generation contracts are unchanged."
  }
}
