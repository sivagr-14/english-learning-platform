export async function seed(): Promise<void> {
  // Vocabulary is intentionally not seeded.
  //
  // Entries must enter the platform through the ChatGPT-controlled workflow:
  // assess -> show exact counts -> approve -> generate -> reconcile.
}
